import React from 'react';

import InputField from '.';
import notes from '../README.md';

export default {
  title: 'Components/InputField',
  parameters: { notes }
};

export const BasicUsage = () => <InputField />;
